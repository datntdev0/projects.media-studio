import { PageSlot } from '../../components/PageSlot';

export function LibraryScreen() {
  return (
    <PageSlot
      title="Library"
      hint="Novels, image sets and video sets — the content catalogue."
    />
  );
}
